<% const label = locals.label || ''; %>
<% const selectText = locals.selectText || ''; %>
<% const selectList = locals.selectList || []; %>

<div class="wrap_my_select">
  <span class="select_label"><%= label %></span>
  <div class="box_my_select">
    <button type="button" class="btn_my_select" aria-expanded="false">
      <span class="text"><%= selectText %></span>
    </button>
    <ul class="list_select_box">
      <% if(selectList && selectList.length > 0) { %>
        <% selectList.forEach(item => { %>
          <li class="item_select_box">
            <button type="button" class="btn_select_item"><%= item %></button>
          </li>
        <% }) %>
      <% } %>
    </ul>
  </div>

</div>